import { App } from './App';

export const Init = () => {
  return (
    <div className="App bg-[length:100%_100%] bg-main bg-no-repeat object-contain  min-h-screen">
      <App />
    </div>
  );
};
